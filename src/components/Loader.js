import React from 'react'
import styled from '@xstyled/styled-components'

const Container = styled.div`
  text-align: center;
`

const Loader = () => <Container>Chargement en cours...</Container>

export default Loader
